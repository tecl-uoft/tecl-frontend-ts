import React, { useReducer } from "react";
import { VideoLinks } from "./videoLinks.json";

type TouchStudyEl = {
  hasRightBar: boolean;
  hasLeftBar: boolean;
  videoURL: boolean;
};

type TouchStudyElResponse = {

};

/* const initialState = { rightBar: "hidden", leftBar: "hidden", videoURL: "" };

function reducer(state: ITouchStudyEl, action) {}

function useTouchStudyEl() {
  const [state, dispatch] = useReducer(reducer, initialState);
}
 */
/* export default StudyHook; */
